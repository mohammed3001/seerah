/**
 * Placeholder root page for the Seerah web app.
 * The infrastructure tier intentionally ships no real UI — see PR description.
 */
export default function HomePage() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>Seerah</h1>
      <p>Infrastructure tier — UI lands in a follow-up PR.</p>
    </main>
  );
}
